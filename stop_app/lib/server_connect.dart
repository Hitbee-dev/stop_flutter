import 'package:flutter/material.dart';

class ServerConnect extends StatelessWidget {
  const ServerConnect({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
